# GENERATED FILE - DO NOT EDIT DIRECTLY.
# Prompt: gen_readme.txt  Version: 1.0
# Generated by: Grok built by xAI
# Timestamp: 2025-08-17T16:32:00Z

from typing import List
import pandas as pd


def gen_readme_df(values: List[List]) -> pd.DataFrame:
    """Creates a DataFrame from a list of lists with README, Value, and Notes columns.

    Args:
        values: A list of lists representing a matrix with at least three columns.

    Returns:
        A pandas DataFrame with columns 'README', 'Value', and 'Notes'.

    Raises:
        TypeError: If values is not a list or contains non-list elements.
        ValueError: If values is empty or any inner list has fewer than three elements.
    """
    # Type validation
    if not isinstance(values, list):
        raise TypeError("values must be a list")

    # Check if values is empty
    if not values:
        raise ValueError("values list cannot be empty")

    # Validate that each element is a list with at least three elements
    for row in values:
        if not isinstance(row, list):
            raise TypeError("Each element in values must be a list")
        if len(row) < 3:
            raise ValueError("Each inner list must have at least three elements")

    # Convert None to empty string and apply str() to all elements
    processed_values = [[str(item) if item is not None else '' for item in row[:3]] for row in values]

    # Create DataFrame
    df_readme = pd.DataFrame(
        processed_values,
        columns=["README", "Value", "Notes"]
    )

    return df_readme